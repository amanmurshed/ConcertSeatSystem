{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Run Concert Seat System",
        "type": "cppdbg",
        "request": "launch",
        "program": "${workspaceFolder}/bin/concert",
        "args": [],
        "stopAtEntry": false,
        "cwd": "${workspaceFolder}",
        "environment": [],
        "externalConsole": true,
        "MIMode": "gdb",
        "miDebuggerPath": "/usr/bin/gdb",  // adjust path if needed
        "setupCommands": [
          {
            "description": "Enable pretty-printing for gdb",
            "text": "-enable-pretty-printing",
            "ignoreFailures": true
          }
        ]
      }
    ]
  }
  